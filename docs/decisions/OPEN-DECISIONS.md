# OPEN-DECISIONS — 悬而未决登记册

> 规则：只追加 + 就地关闭（OPEN → RESOLVED，补 Resolution 字段）。
> 每个 Phase 开始时，未决项自动复现到工作上下文最前面，逐条判断能否关闭。
> 已关闭项可升格为 ADR。

| Date | Source | Open Item | Related Constraints | Current Leaning | Blocked By | Resolves When | Status |
|------|--------|-----------|---------------------|-----------------|------------|---------------|--------|
| 2026-09-16 | Phase 0 数据核对 | 垫付文本声明"共 7 笔"但明细仅 5 条（金额 2250 自洽），差 2 笔的性质未定 | 决定 `COUNT_MISMATCH` 定档 P0 阻断还是 P1 警告 | 按 P0 阻断：宁可拦住，不可漏填 | ~~需运营方确认数据来源~~ | 用户已授权按默认处置 | **RESOLVED（2026-09-16，用户确认）** |
| 2026-09-16 | Phase 0 数据核对 | 账号表已有抖加记录 400 元（高高 100/张栋梁 200/葵花夫妇 100）与本次申请 700 元是叠加还是替代 | 决定跨源校验级别与当日汇总口径 | 技术处置已定（见下 R-02）：降 P1 不阻断、禁止自动调和；业务上按"叠加但标注" | ~~需运营方确认批次口径~~ | 用户已授权按默认处置 | **RESOLVED（2026-09-16，用户确认）** |
| 2026-09-16 | Phase 0 数据核对 | 支付人口径：文本"爆火音乐"（公司主体）与表中"李老师/木木老师/林老师"（个人）是并列还是不同环节；"林老师（预计代付）"与"木木老师（历史已付）"是否同角色 | 决定 `PAYER_INCONSISTENT` / 跨源支付人冲突的定档 | 技术处置已定（见下 R-03）：归入 P1 口径待确认，不假设 | ~~需运营方确认支付链路~~ | 用户已授权按默认处置 | **RESOLVED（2026-09-16，用户确认）** |
| 2026-09-16 | Phase 0 需求澄清 | 表格"发布日期"使用 `4.9` / `4.10` 无年份写法，年份归属需确认 | 决定日期归一化是否需人工介入 | 归一化带 `inferred: true` 标记并置待确认；无法唯一确定时抛 `DATE_UNRESOLVABLE`（P0） | ~~需确认台账年份~~ | 用户已授权按默认处置 | **RESOLVED（2026-09-16，用户确认）** |

> **收口说明（2026-09-16，项目总监记录）**：用户在 Spec v1.0 评审时明确回复「Spec 确认，Q2~Q6 都按你的默认处置」。
> 上述 4 条即 Q2~Q6 中的业务未决项，**按各条 Current Leaning 落地，不再等待运营方逐一确认**。
> 由此产生的工程后果被显式固化，而不是被忽略：
> - 笔数差 — 定档 P0 阻断（宁可拦住）；演示中被裁决为「以明细为准」后放行。
> - 抖加 400 vs 700 — **降 P1 不阻断 + 禁止自动调和**（`CROSS_SOURCE_TOTAL_MISMATCH`）；三口径并列展示，不合并为一个数字。
> - 支付人口径 — **不假设、不自动对齐**；只有「同一记录同一字段取值不同」才升级为 `CROSS_SOURCE_FIELD_CONFLICT`（P0），其余归 `CROSS_SOURCE_SCOPE_MISMATCH`（P1）。
> - 发布日期年份 — 归一化时打 `inferred` 标记并作为 P1 提示；无法唯一确定时才抛 `DATE_UNRESOLVABLE`（P0）。
>
> 若后续运营方给出不同口径，以上任一条都可单独回退——**回退成本被限制在单个异常码的定档变更**，不影响解析与写入链路。

## 关闭记录

| Date | Source | Open Item | Resolution | 升格 |
|------|--------|-----------|------------|------|
| 2026-09-16 | Phase 1 架构师 OPEN | 测试副本写入策略："覆盖" 还是 "追加" | **RESOLVED → 追加 + 溯源列**。原表只读，副本需保留每次写入轨迹，覆盖会破坏可追溯性。溯源列采用下划线前缀（`_batch_id / _request_id / _rule_id / _confirmed_by / _confirmed_at / _dedup_key`），与业务 13 列并存不污染。 | ADR-003 |
| 2026-09-16 | Phase 1 架构师 OPEN | 演示态存储选型：内存 还是 SQLite | **RESOLVED → 内存为主 + 裁决日志落 JSONL**。主数据用内存（演示够用、零依赖）；append-only 裁决日志落 JSONL，使重启后仍可展示裁决轨迹。若 QA 需要跨重启保留主数据再评估升级。 | ADR-003 |
| 2026-09-16 | Phase 1 一致性检查（项目总监裁决 R-02） | 跨源总额校验 `CROSS_SOURCE_TOTAL_MISMATCH` 级别定为 P0 阻断，与"抖加批次应零 P0"断言矛盾 | **RESOLVED → 降为 P1**，保留"禁止自动调和、必须人工确认口径"。理由：账号表「抖加」列无批次/日期维度，无法证明表内 400 与文本 700 同批次；对无法验证的等式施加阻断会使每批正常申请无法落表（表内累计必然大于单日声明额），阻断常态化即阈值失效。 | ADR-001（校验分级） |
| 2026-09-16 | Phase 1 一致性检查（项目总监裁决 R-03） | 「文本支付人爆火音乐 vs 表内高高=李老师」被列为 P0 冲突 | **RESOLVED → 归入 `CROSS_SOURCE_SCOPE_MISMATCH`（P1）**。理由：不满足"同一博主同一字段、值不同"的真冲突条件——高高不在本批明细内，不同博主由不同人支付可以并存。`CROSS_SOURCE_FIELD_CONFLICT` 保持 P0，但适用条件收紧为同记录同字段值冲突。 | ADR-001（校验分级） |
| 2026-09-16 | Phase 1 设计师 advisory | 负责人汇报的关注点排序（当日金额 / 异常项 / 待决策项） | **RESOLVED → 异常与待决策项优先，金额次之**。汇报卡压缩为"一行结论 + 3~4 个关键数字 + ≤3 条需决策项（严重度徽章 + 一句话 + 需谁的什么决定）"，超出折叠为"另有 N 项"。 | — |
| 2026-09-16 | Phase 1 设计师 advisory | 产品命名待定 | **RESOLVED → 暂用"推广申请处理台"占位**，最终名称由 Spec v1.0 锁定。 | — |
| 2026-09-16 | Phase 1 设计师 advisory | 文本类型 Tab 是否仅"抖加/垫付"两类 | **RESOLVED → MVP 仅两类**，组件结构预留扩展位（新增类型不得改契约）。 | — |

## 待办：需在 Spec v1.0 中转为 ADR 的悬置项

- `COUNT_MISMATCH` 的最终定档（P0 / P1）依赖业务确认——若运营确认"7 笔"为口径错误而非数据丢失，可降级为 P1 并同步修订 ADR-001。
