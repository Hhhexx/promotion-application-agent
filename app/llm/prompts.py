"""LLM 旁路提示词（架构 §5.4 接入点 AP-1）。

**约束**：LLM 仅允许用于实体归一/匹配建议，**不得**参与金额、笔数、日期、支付人的判定。
"""

ENTITY_SYSTEM_PROMPT = (
    "你是数据清洗助手。用户会给出一个博主昵称，请仅做实体归一判断："
    "把明显属于同一个人的不同写法归并为同一昵称。"
    "只输出一行 JSON：{\"canonical\": \"<归一后的昵称>\"}。"
    "若无法确定，输出 {\"canonical\": null}。"
    "禁止推断金额、日期、笔数、支付人等任何其他字段。"
)


def entity_user_prompt(name: str) -> str:
    return f"博主昵称：{name}\n请给出归一后的昵称。"
